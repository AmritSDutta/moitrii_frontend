---
title: Moitrii Documentation
description: Affordable personal AI agent platform for modern Indian women — lifestyle companion built on React/Next.js and Convex Cloud.
---

# Moitrii — Personal AI Companion for Women

Moitrii is an affordable personal AI agent platform built for the modern Indian woman. Every user gets a dedicated, persistent personal AI agent that sleeps between cycles and wakes on a schedule determined by the subscription tier to research, generate, or reuse lifestyle content.

## Core Principle

All users receive **identical** agent capabilities. Subscription tiers determine only **execution frequency** (wake periodicity) — not feature access.

## Key Features

- **Sleeping Agent Model** — Agent sleeps and wakes periodically based on subscription tier
- **Shared Knowledge Ecosystem** — Agents reuse verified guides to avoid duplicate generation costs
- **Convex Realtime Backend** — End-to-end TypeScript with type-safe queries, mutations, and full-text search
- **Google OAuth Authentication** — Built on `@convex-dev/auth`
- **Editorial Lifestyle Aesthetic** — Warm, calm, magazine-like UI distinct from generic SaaS dashboards

## Technology Stack

| Layer | Technology |
|-------|-----------|
| Frontend | Next.js 14 (App Router), React 18, TypeScript |
| Styling | Tailwind CSS 3, custom editorial color palette |
| Backend | Convex Cloud (Database, Queries, Mutations, File Storage) |
| Auth | `@convex-dev/auth` with Google OAuth |
| Icons | Lucide React |

## Quick Start

```bash
npm install
npm run dev
```

This starts the Next.js dev server at `http://localhost:3000`. You also need `npx convex dev` to run the Convex backend locally (requires `NEXT_PUBLIC_CONVEX_URL` in `.env.local`).

## Documentation Sections

- **Getting Started** — Local setup and development workflow
- **Architecture** — System overview and data flow
- **Data Model** — Convex schema and table relationships
- **Design System** — Color palette, typography, and component conventions
- **Screens** — UI screen specifications and user journeys
- **Convex API** — Reference for all backend queries and mutations
- **FAQ** — Troubleshooting common local setup issues
