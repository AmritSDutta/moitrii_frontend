---
title: UI Screens
description: Reference for all six core user-facing screens, routes, and their user journeys.
---

# UI Screens

Moitrii has six core user-facing screens covering the full lifestyle agent lifecycle.

## 1. Public Landing & Explore (`/`)

**Access:** Public (no auth required)

- **Persistent Header & Navigation:** Responsive navigation with persistent Sign In button across all viewports (mobile, tablet, desktop) and editorial topic links (`?category=...#articles-section`)
- Hero section with warm lifestyle banner, search bar, and popular search pills
- "What's Hot" — featured articles sorted by reuse count
- "Popular Categories" — interactive topic grid cards
- "Latest Stories" — browsable article grid with reuse badges and dynamic category filtering
- "Trending Videos" — curated YouTube video cards with modal player
- "About Moitrii" mission footer with multi-language greetings and weekly curated lifestyle digest subscription input (subscribers mutation with Brevo welcome trigger)

## 2. Onboarding & Interest Selection (`/onboarding`)

**Access:** Auth required (AuthGuard)

- Two-column topic grid split into:
  - **Core Lifestyle & Mindful Living** (lifestyle + wellness categories)
  - **Parenting & Youth Culture** (parenting category — Gen-Z, Anime, Comics)
- Interactive toggle cards with icon, description, and popular search tags
- Floating summary bar showing selected topic count
- "Save & Go to Dashboard" action persists interests to Convex and navigates to `/dashboard`

**Topics Available:**

| Topic | Category | Icon |
|-------|----------|------|
| Health & Nutrition | wellness | Heart |
| Cooking & Recipes | lifestyle | Utensils |
| Beauty & Skincare | lifestyle | Sparkles |
| Yoga & Fitness | wellness | Activity |
| Mindful Wellness | wellness | Sun |
| Kids & Education | parenting | GraduationCap |
| Home & Living | lifestyle | Home |
| Travel & Escapes | lifestyle | Compass |
| Gen-Z Trends | parenting | Zap |
| Anime & Manga | parenting | Smile |

## 3. Agent Dashboard (`/dashboard`)

**Access:** Auth required (AuthGuard)

- **Greeting Hero:** Personalized "Good Morning, {name}" with agent companion logo
- **Agent Companion Box:**
  - Dedicated virtual agent identity (`Moitrii Companion`)
  - Dedicated virtual inbox badge (`agent-...@agentmail.to`)
  - Live status badge (`SLEEPING` / `ACTIVE` / `WORKING` / `WAITING` / `ERROR`)
  - Status message, subscription tier, and demo "Simulate Wake Cycle" button
- **Wake-Up Schedule Card:** Next scheduled wake time (defaults to 11:00 PM IST), frequency, last active timestamp, and schedule time modification (configurable to any 24h time of day in IST)
- **Preferred Language Selector:** Card positioned directly below schedule with native script toggle pills for **English** (`en`), **বাংলা** (`bn`), and **हिन्दी** (`hi`)
- **Quick Request Composer:** Natural language prompt input with category dropdown → `createRequest` mutation
- **Active Interests Strip:** Followed topics as pills with edit link to `/onboarding`
- **Agent Work Feed:**
  - **Ready to Read** — completed deliverables with "Read Guide" link to `/content/[id]`
  - **Queued Tasks** — pending/processing requests with wake window indicator

## 4. Request Center & History (`/requests`)

**Access:** Auth required (AuthGuard)

- **Request Composer:** Textarea with topic selector → `createRequest` mutation
- **Filter Pills:** All / PENDING / COMPLETED
- **Request History List:** For each request shows:
  - Status badge (`PENDING` / `PROCESSING` / `COMPLETED` / `FAILED`)
  - Category tag
  - Prompt text
  - Delivered guide title (if completed)
  - Reuse status: "Shared Knowledge Reuse" vs "Freshly Researched & Synthesized"
  - Submission timestamp
  - Link to content reader (if completed)

## 5. Content Reader (`/content/[id]`)

**Access:** Public

- Back button navigating to previous page
- Header: category badge, reuse count badge, title
- **Audio Narration Bar:** Positioned directly below the title; displays interactive play/pause controls when `audioUrl` is present, or disabled indicator ("Audio narration not available") when omitted
- Subtitle and metadata row:
  - Author byline with **Author Type Badge** (`AI Agent Companion` vs `Human Author`)
  - Read time, publish date, share button
- **AI Synthetic Content Disclaimer:** Prominent editorial disclaimer banner rendered directly under the metadata row when content is AI/agent-generated (*"Disclaimer: AI generated synthetic content synthesized by Moitrii Agent."*)
- Editorial cover photo (16:9 aspect ratio)
- **Agent Key Takeaways** — bulleted list with check icons
- **Video Companion** — embedded YouTube player (if `youtubeId` present)
- **Article Body** — markdown-rendered content with `###` and `####` subsections
- **Verified Sources** — citation list with external links
- **Related Stories** — two related article cards from the shared ecosystem

## 6. Publisher Studio (`/publisher`)

**Access:** Auth required (AuthGuard)

- **Mode Toggle:** Compose / Live Preview
- **Article Meta Form:** Title, subtitle, category, author byline, read time
- **Cover Image & Media:** URL input or file upload to Convex File Storage, optional Audio Narration URL
- **YouTube:** Optional video ID and title fields
- **Key Takeaways Builder:** Add/edit/remove takeaway bullets (numbered)
- **Markdown Editor:** Textarea for article body with live preview
- **Publish Action:** Calls `publishContent` mutation → redirects to `/content/[slug]`

## 7. Scheduled Agent Activities & Shared Knowledge

**Access:** Fully automatic (no user interaction required)

Moitrii's agent operates on a **sleeping agent model** with autonomous scheduled wake cycles managed by Convex Cron jobs. The entire pipeline is designed around a shared knowledge ecosystem that rewards reuse and avoids duplicate generation.

### Sleep / Wake Cycle

- Each agent has a user-configurable wake schedule in IST (default **11:00 PM IST**, fully customizable to any 24h time of day in IST).
- A Convex cron job (`convex/crons.ts`) fires **hourly using `crons.hourly`**, with `minuteUTC` omitted so Convex assigns an off-peak minute and spreads execution away from the top of the hour.
- The cron dispatches `triggerScheduledWakes` for every agent whose scheduled wake time falls inside the **60-minute dispatch window** that just elapsed.
- Agents without pending requests are skipped — the scheduler is request-driven, not time-driven alone.

### The Autonomous AI Pipeline (per due agent)

When an agent is due and has queued `PENDING` requests, the runner executes a 6-step pipeline:

1. **Content Reuse Check** (`convex/ai/reuseEngine.ts`) — Full-text search against the shared knowledge library. If an existing guide matches the prompt with high confidence, the agent **reuses it** instead of regenerating (incrementing `reusedCount` on the published guide — the social sharing signal).
2. **Multilingual Synthesis** (`convex/ai/synthesizer.ts`) — Generates a novel editorial guide in the user's preferred language (**English / বাংলা / हिन्दी**) with citations and key takeaways.
3. **Audio Narration** (`convex/ai/tts.ts`) — Synthetic speech generation stored in Convex `_storage`; linked to the published guide for in-article playback.
4. **Video Discovery** (`convex/ai/research.ts`) — Curates relevant YouTube companion videos matched by category and prompt.
5. **Personal Notification** (`convex/ai/agentMail.ts`) — Sends an HTML research report from the user's dedicated agent inbox (`agent-<userId>@agentmail.to`) to their personal email via AgentMail.
6. **Atomic Publish** (`convex/content.ts` → `internalPublishGuide`) — Writes the deliverable into the **Shared Knowledge Library** (available to all users for reuse).

### Social Knowledge Reuse

The shared knowledge ecosystem is Moitrii's core social efficiency layer:
- When multiple users request similar topics, the first agent to publish a guide makes it available for **reuse** by subsequent agents — saving LLM cost and building a growing library of verified lifestyle content.
- Every reused guide's `reusedCount` increments, powering the "What's Hot" trending rankings on the landing page.
- Content reuse is transparent to the user: request history distinguishes **"Shared Knowledge Reuse"** from **"Freshly Researched & Synthesized"**.

### Failure Rollback

If any pipeline step fails (LLM error, storage failure, email dispatch error):
- The agent is automatically reverted to **SLEEPING** with an explanatory status message.
- All affected requests are reset to **PENDING** so they are picked up by the next wake cycle — no request is ever lost or stuck in `PROCESSING` forever.
