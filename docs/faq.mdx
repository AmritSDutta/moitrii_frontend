---
title: FAQ
description: Troubleshooting common local setup issues — Vite development, missing dev dependencies, Vitest runtime errors, and Convex codegen.
---

# FAQ

## How is authentication handled in the pure React SPA?

Authentication is powered by `@convex-dev/auth/react`. The SPA root (`src/components/ConvexClientProvider.tsx`) wraps the application tree with `ConvexAuthProvider`, which manages token exchange and user session state via React Context. Member routes (`/dashboard`, `/requests`, `/onboarding`, `/publisher`) are guarded by `AuthGuard`, while public routes (`/`, `/explore`, `/content/:id`) render immediately for all visitors.

## `npm install` deletes dev dependencies

**Symptom:** After `npm install`, packages such as `vitest`, `tailwindcss`, or `typescript` are gone from `node_modules`, and `npm test` fails.

**Cause:** npm is configured with `omit=dev`, which it inherits automatically if a global `NODE_ENV=production` is present in your environment.

**Fix:** Install with dev dependencies explicitly included:

```bash
npm install --include=dev
```

## How do environment variables work in Vite vs Convex?

- **Frontend Variables:** Vite exposes variables prefixed with `VITE_` via `import.meta.env` (e.g., `VITE_CONVEX_URL`).
- **Convex Backend Variables:** Backend credentials (`AUTH_GOOGLE_ID`, `AUTH_GOOGLE_SECRET`, `BREVO_API_KEY`, `AGENTMAIL_API_KEY`, `OPENAI_API_KEY`, `SARVAM_API_KEY`, `FIRECRAWL_API_KEY`) are configured directly in your Convex Cloud Dashboard for deployment `brazen-rook-983`.


## Tests fail with `act(...) is not supported in production builds of React`

**Symptom:** UI tests throw `Error: act(...) is not supported in production builds of React` as soon as `render()` is called.

**Cause:** React resolves to its **production** build, because `NODE_ENV=production` is set when the test runner boots.

**Fix:** `vitest.config.ts` already forces the test environment via `test.env`:

```ts
test: {
  env: { NODE_ENV: "test" },
  // ...
}
```

If you run Vitest from another entry point (a custom script, an IDE runner), make sure `NODE_ENV` is not `production`.

## Where do the tests live, and how do I run them?

Two tiers run under one config:

| Tier | Location | Environment | What it covers |
|------|----------|-------------|----------------|
| UI | `src/**/*.test.{ts,tsx}` | `jsdom` | Components/pages with `convex/react` and auth hooks mocked |
| Convex | `convex/**/*.test.ts` | `edge-runtime` | Real functions via `convex-test` against an in-memory backend |

```bash
npm test          # run both projects once
npm run test:watch  # watch mode
```

## Convex functions are missing types / `_generated` is empty

**Symptom:** Imports from `convex/_generated/api` fail, or the app cannot resolve any Convex function.

**Cause:** `convex/_generated/` is gitignored, so a fresh clone has no generated client until Convex codegen runs.

**Fix:** start the Convex dev server once (or run codegen) to generate it:

```bash
npx convex dev
```

## Why are subscriber emails and agent notifications sent via different providers?

Moitrii uses a **dual-channel messaging separation**:
- **Brevo (`convex/emails/brevo.ts`):** Handles marketing, subscriber welcome emails, and batch weekly lifestyle digests. These are platform-wide broadcasts directed to users on the `subscribers` list.
- **AgentMail (`convex/ai/agentMail.ts`):** Handles 1:1 personal agent deliverables. Every user has a dedicated personal companion (`Moitrii Companion`). As of now, sender address is fixed to `moitrii@agentmail.to` due to AgentMail's free-tier single custom inbox constraint. The production architecture is planned for 1 dedicated virtual AgentMail address dynamically provisioned per user (`agent-${userId.slice(-6)}@agentmail.to`). Research deliverables are delivered directly from the companion identity to the user's email, maintaining context and personalization.

## How does Moitrii handle user data privacy and PII anonymization?

To ensure strict privacy and future anonymization readiness:
1. **Isolated PII:** Real user names and personal emails exist solely in the `users` table.
2. **Generic Companion Personas:** The `agents` table stores only persona name `"Moitrii Companion"` and the companion AgentMail address (`moitrii@agentmail.to`). It never stores the user's real name or email.
3. **Opaque References:** All other tables (`requests`, `content`, `userInterests`) reference users only through opaque database IDs (`userId: v.id("users")`).
4. **No Prompt Exposure on Public Routes:** `/content/:slug` is public, so `getContentBySlug` returns an explicit projection rather than the stored document. The raw request text (`content.generatedFromPrompt`) is never returned by any query.
5. **Retention:** Requests carry a 3-day TTL and are removed by the daily `cleanupExpiredRequests` cron. Published guides are permanent, and the slug derived from a request can be a public URL — the full picture, including the accepted risks, is on [Privacy & Data Handling](/privacy).


## Why were images missing on reload with `net::ERR_FILE_NOT_FOUND`, and how is it prevented?

**Symptom:** After publishing an article with a local file upload and refreshing the page, the cover image fails with `net::ERR_FILE_NOT_FOUND` on a URL like `blob:http://localhost:3000/be574934-5c42-...`.

**Cause:** `URL.createObjectURL(file)` creates temporary in-memory browser URLs that become invalid upon page refresh or session restart. If stored directly in the database as the permanent `coverImage` string, reloads fail.

**Fix:**
1. **Convex File Storage Resolution:** In `convex/content.ts`, `publishContent` and `internalPublishGuide` accept `coverImageStorageId` from Convex storage. Query functions (`getContentBySlug`, `getPublishedContent`, `getWhatsHot`) resolve this ID to a permanent CDN URL via `ctx.storage.getUrl(doc.coverImageStorageId)`.
2. **Blob Sanitization:** Any legacy `blob:` string stored in the database is automatically sanitized by Convex queries to a high-resolution fallback image.
3. **Frontend Fallback:** `ContentReaderPage.tsx` and `HomePage.tsx` include `onError` handlers that default to `/images/moitrii.jpg` if an external image link fails to load.

## Why were YouTube companion videos failing to play with "Video unavailable"?

**Symptom:** The embedded video companion in articles or trending videos modal fails to play or displays "Video unavailable".

**Cause:** Embed iframes require an exact 11-character video ID. If users or publishers paste full watch links (`https://www.youtube.com/watch?v=...`), short URLs (`https://youtu.be/...`), or invalid mock placeholders (such as `input-yoga-sample`), passing them directly into `https://www.youtube.com/embed/${id}` creates a malformed URL.

**Fix:**
1. **YouTube ID Extractor:** `src/lib/youtube.ts` provides `extractYouTubeId()` which parses standard watch URLs, short links, embed links, and raw IDs using regex to extract the clean 11-character ID.
2. **Safe Embed Rendering:** `ContentReaderPage.tsx` and `HomePage.tsx` wrap all iframe embeds with `extractYouTubeId()`, safely omitting or disabling the player if the video ID is invalid or missing.

## Why does refreshing a subroute on Cloudflare Pages return 404 Not Found?

**Symptom:** Opening `https://<app>.pages.dev` works, but directly refreshing or pasting a link to `https://<app>.pages.dev/dashboard`, `https://<app>.pages.dev/requests`, or `https://<app>.pages.dev/content/:slug` returns an HTTP 404 error.

**Cause:** React Router handles routing entirely on the client side (in the browser). When you refresh or type a subroute URL directly, the browser asks Cloudflare Pages for a file at `/dashboard.html` or `/dashboard/index.html`. Since Vite creates a single static `index.html` at the root of `dist/`, Cloudflare Pages does not find a static file matching that path and serves a 404 error.

**Fix:**
Configure a single-page-application rewrite so unmatched paths serve `/index.html` with an HTTP 200 status:
```text
/*    /index.html   200
```
On Cloudflare Pages this is the project's **SPA fallback** setting (Settings → Builds & deployments), which is how the deployed app is configured today. The identical rule can also be checked in as `public/_redirects` — Vite copies `public/` into `dist/` during `npm run build`, so the file ships with the build and Pages applies it. Either way React Router boots and renders the correct subpage.

Because the rewrite matches every path, any file that must be served verbatim has to exist in the build output — which is why `public/robots.txt` and `public/sitemap.xml` are checked in rather than served through the fallback.

## How are Audio Summaries and Infographic Covers generated?

1. **Audio Summaries (`convex/ai/tts.ts`):**
   - **Primary Engine:** Sarvam AI Bulbul v3 (`bulbul:v3`) with Indic pacing, emotional prosody, and natural audio synthesis (2 retry attempts).
   - **Fallback 1:** OpenAI `tts-1` (`nova` voice) if Sarvam encounters rate limits or network issues.
   - **Fallback 2:** Offline 44-byte silent WAV generation ensuring storage persistence is always resilient and non-blocking.
   - **Storage:** Generated audio is stored in Convex File Storage and attached directly to the published article.

2. **Infographic Cover Images (`convex/ai/imagegen.ts`):**
   - **Format:** 16:9 horizontal landscape (`1536x1024`) pictorial infographic poster.
   - **Strict Visual Rules:** Enforces 100% visual and pictorial depiction with **strictly no text, words, or typography**.
   - **Modesty & Safety Ethics:** Strict guardrails ensure family-safe, culturally respectful lifestyle imagery with zero vulgarity.
   - **Fallback:** Curated high-resolution category covers if image generation is disabled or unavailable.


